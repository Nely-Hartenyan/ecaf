# College Site

Веб-приложение для управления контентом колледжа, построенное на Next.js с использованием PostgreSQL и Prisma.

## Требования

- Node.js 18+
- npm, yarn, pnpm или bun
- Docker и Docker Compose (для локальной базы данных)

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd college-site
```

### 2. Установка зависимостей

```bash
npm install
# или
yarn install
# или
pnpm install
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp env.example .env
```

Или создайте `.env` вручную со следующим содержимым:

```env
# Database
DATABASE_URL="postgresql://college_user:college_password@localhost:5432/college_db?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"
```

**Важно:** Для генерации безопасного `NEXTAUTH_SECRET` выполните:

```bash
openssl rand -base64 32
```

### 4. Запуск базы данных

Запустите PostgreSQL в Docker:

```bash
npm run db:up
# или
docker-compose up -d
```

Это создаст и запустит контейнер PostgreSQL с предустановленными настройками.

### 5. Настройка базы данных

Выполните миграции и заполните базу данных начальными данными:

```bash
# Генерация Prisma Client
npm run db:generate

# Применение миграций
npm run db:migrate

# Заполнение базы данных тестовыми данными
npm run db:seed
```

**Или используйте автоматическую настройку:**

```bash
npm run setup
```

Эта команда выполнит все шаги выше автоматически.

### 6. Запуск приложения

```bash
npm run dev
# или
yarn dev
# или
pnpm dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Полезные команды

### Работа с базой данных

```bash
# Запустить PostgreSQL
npm run db:up

# Остановить PostgreSQL
npm run db:down

# Полный сброс базы данных (удаляет все данные!)
npm run db:reset

# Применить миграции (создаст базу данных, если её нет)
npm run db:migrate

# Сгенерировать Prisma Client
npm run db:generate

# Заполнить базу данных тестовыми данными
npm run db:seed

# Открыть Prisma Studio (GUI для базы данных)
npm run db:studio
```

### Разработка

```bash
# Запустить dev сервер
npm run dev

# Собрать для production
npm run build

# Запустить production сервер
npm run start

# Проверить код линтером
npm run lint
```

## Структура проекта

```
college-site/
├── app/                    # Next.js App Router
│   ├── (public)/          # Публичные страницы
│   ├── admin/             # Админ-панель
│   └── api/               # API routes
├── server/                 # Server actions
├── lib/                   # Утилиты и конфигурация
├── prisma/                # Prisma схема и миграции
└── public/                # Статические файлы
```

## Учетные данные для входа

После выполнения `npm run db:seed` будут созданы следующие тестовые аккаунты администратора:

- Email: `admin1@college.am` / Password: `Admin12345!`
- Email: `admin2@college.am` / Password: `Admin12345!`
- Email: `admin3@college.am` / Password: `Admin12345!`

**Важно:** Измените пароли в production окружении!

## Решение проблем

### База данных не подключается

1. Убедитесь, что Docker запущен
2. Проверьте, что контейнер работает: `docker ps`
3. Проверьте логи: `docker-compose logs postgres`
4. Убедитесь, что `DATABASE_URL` в `.env` соответствует настройкам в `docker-compose.yml`

### Ошибки миграций

Если возникают проблемы с миграциями:

```bash
# Сбросить базу данных и начать заново
npm run db:reset
```

### Порт 5432 уже занят

Если порт 5432 уже используется, измените порт в `docker-compose.yml`:

```yaml
ports:
  - "5433:5432" # Используйте другой порт
```

И обновите `DATABASE_URL` в `.env` соответственно.

## Технологии

- **Next.js 16** - React фреймворк
- **PostgreSQL** - База данных
- **Prisma** - ORM
- **NextAuth** - Аутентификация
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация

## Деплой

📖 **Подробная инструкция по деплою:** см. [DEPLOY.md](./DEPLOY.md)

### Деплой на Vercel (Рекомендуется)

#### 1. Подготовка репозитория

Убедитесь, что ваш код находится в Git репозитории (GitHub, GitLab, Bitbucket).

#### 2. Создание проекта на Vercel

1. Зайдите на [vercel.com](https://vercel.com) и войдите через GitHub/GitLab
2. Нажмите "Add New Project"
3. Импортируйте ваш репозиторий
4. Vercel автоматически определит Next.js проект

#### 3. Настройка базы данных PostgreSQL

**Вариант A: Vercel Postgres (Проще всего)**

1. В настройках проекта Vercel перейдите в раздел "Storage"
2. Нажмите "Create Database" → выберите "Postgres"
3. Создайте базу данных
4. Vercel автоматически добавит переменную `POSTGRES_PRISMA_URL` в Environment Variables

**Вариант B: Внешний провайдер (Neon, Supabase, Railway и т.д.)**

1. Создайте базу данных на выбранном провайдере
2. Скопируйте connection string
3. Добавьте его как `DATABASE_URL` в Environment Variables на Vercel

#### 4. Настройка переменных окружения

В настройках проекта Vercel → "Environment Variables" добавьте:

```env
# База данных (если используете внешний провайдер)
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# Или используйте автоматически созданную Vercel Postgres
# POSTGRES_PRISMA_URL (создается автоматически при использовании Vercel Postgres)
# POSTGRES_URL_NON_POOLING (создается автоматически)

# NextAuth
NEXTAUTH_URL="https://your-domain.vercel.app"
NEXTAUTH_SECRET="your-secret-key-here"  # Сгенерируйте: openssl rand -base64 32
```

**Важно:**

- Для `NEXTAUTH_URL` используйте ваш production домен
- Для `NEXTAUTH_SECRET` сгенерируйте новый секретный ключ (не используйте тот же, что в development)

#### 5. Настройка Prisma для Vercel Postgres

Если используете Vercel Postgres, нужно обновить `lib/db.ts` для использования правильной переменной окружения. Vercel создает две переменные:

- `POSTGRES_PRISMA_URL` - для Prisma (с connection pooling)
- `POSTGRES_URL_NON_POOLING` - для прямых подключений

#### 6. Настройка миграций

Vercel автоматически выполнит `prisma generate` благодаря скрипту `postinstall` в `package.json`.

Для применения миграций при деплое, добавьте в настройках проекта Vercel → "Build & Development Settings" → "Build Command":

```bash
prisma generate && prisma migrate deploy && next build
```

Или создайте файл `vercel.json`:

```json
{
  "buildCommand": "prisma generate && prisma migrate deploy && next build"
}
```

#### 7. Первый деплой

1. Нажмите "Deploy" на Vercel
2. После успешного деплоя выполните миграции вручную (если не настроили автоматически):

```bash
# Подключитесь к production базе данных
npx prisma migrate deploy
```

#### 8. Заполнение базы данных

После первого деплоя выполните seed (опционально):

```bash
# Установите DATABASE_URL для production
export DATABASE_URL="your-production-database-url"
npx prisma db seed
```

**✅ Загрузка файлов настроена автоматически**

Код уже настроен для работы с Vercel Blob Storage!

- **Локально (development):** Файлы сохраняются в `public/uploads/` (работает без настройки)
- **На Vercel (production):** Файлы автоматически сохраняются в Vercel Blob Storage

**Для работы на Vercel:**

1. Создайте Blob Storage в настройках проекта Vercel → "Storage" → "Create Database" → "Blob"
2. Vercel автоматически добавит переменную `BLOB_READ_WRITE_TOKEN`
3. Готово! Загрузка файлов будет работать автоматически

Подробнее см. [DEPLOY.md](./DEPLOY.md)

### Деплой на другие платформы

#### Railway

1. Подключите репозиторий к Railway
2. Добавьте PostgreSQL service
3. Настройте переменные окружения
4. Railway автоматически задеплоит проект

#### Render

1. Создайте новый Web Service
2. Подключите репозиторий
3. Добавьте PostgreSQL database
4. Настройте переменные окружения
5. Укажите Build Command: `prisma generate && prisma migrate deploy && npm run build`

#### Self-hosted (VPS)

1. Установите Node.js, PostgreSQL на сервере
2. Клонируйте репозиторий
3. Настройте `.env` файл
4. Выполните миграции: `npm run db:migrate:deploy`
5. Запустите: `npm run build && npm start`
6. Используйте PM2 или systemd для управления процессом

### Чеклист перед деплоем

- [ ] Все переменные окружения настроены
- [ ] База данных создана и доступна
- [ ] `NEXTAUTH_SECRET` сгенерирован и установлен
- [ ] `NEXTAUTH_URL` указывает на production домен
- [ ] Миграции применены к production базе
- [ ] Vercel Blob Storage создан (если деплоите на Vercel)
- [ ] Тестовые данные удалены или изменены пароли
- [ ] Проверена работа приложения в production режиме локально: `npm run build && npm start`

## Лицензия

MIT
