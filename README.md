# College Site - Система управления контентом колледжа

Веб-приложение для управления контентом колледжа, построенное на Next.js с использованием PostgreSQL и Prisma.

## 🚀 Быстрый старт для новых разработчиков

### Предварительные требования

- Node.js 18+ ([скачать](https://nodejs.org/))
- Git ([скачать](https://git-scm.com/))
- Vercel CLI (установится автоматически при необходимости)

### Шаг 1: Клонирование репозитория

```bash
git clone git@github.com:Nely-Hartenyan/ecaf.git
cd ecaf
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

### Шаг 3: Настройка переменных окружения

#### Вариант A: Использовать базу данных Vercel (РЕКОМЕНДУЕТСЯ)

1. **Установите Vercel CLI:**

   ```bash
   npm i -g vercel
   ```

2. **Войдите в Vercel:**

   ```bash
   vercel login
   ```

3. **Подключитесь к проекту:**

   ```bash
   vercel link
   ```

   - Выберите существующий проект
   - Или создайте новый (если у вас есть права)

4. **Получите переменные окружения из Vercel:**

   ```bash
   vercel env pull .env.local
   ```

   Это создаст файл `.env.local` с переменными из production, включая:

   - `DATABASE_URL` - подключение к базе данных Vercel
   - `NEXTAUTH_SECRET` - секретный ключ
   - `NEXTAUTH_URL` - URL приложения
   - `BLOB_READ_WRITE_TOKEN` - токен для загрузки файлов

5. **Проверьте, что файл создан:**
   ```bash
   cat .env.local | grep DATABASE_URL
   ```

#### Вариант B: Использовать локальную базу данных

1. **Скопируйте пример файла окружения:**

   ```bash
   cp env.example .env.local
   ```

2. **Отредактируйте `.env.local`** и настройте локальную базу данных:

   ```env
   DATABASE_URL="postgresql://user:password@localhost:5432/college_db?schema=public"
   NEXTAUTH_URL="http://localhost:3000"
   NEXTAUTH_SECRET="your-secret-key-here"
   ```

3. **Запустите локальную базу данных (Docker):**

   ```bash
   npm run db:up
   ```

4. **Примените миграции:**

   ```bash
   npm run db:migrate
   ```

5. **Заполните базу данных (опционально):**
   ```bash
   npm run db:seed
   ```

### Шаг 4: Запуск проекта

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

✅ **Готово!** Проект запущен локально.

---

## 📋 Полезные команды

### Работа с базой данных

```bash
# Получить переменные из Vercel (обновить .env.local)
vercel env pull .env.local

# Открыть Prisma Studio (GUI для просмотра БД)
npm run db:studio

# Применить миграции (только для локальной БД)
npm run db:migrate

# Заполнить базу данных тестовыми данными (только для локальной БД)
npm run db:seed

# Создать администратора (работает с любой БД)
npm run db:create-admin
```

### Разработка

```bash
# Запустить dev сервер
npm run dev

# Собрать проект
npm run build

# Запустить production сервер
npm start

# Проверить код линтером
npm run lint
```

### Локальная база данных (Docker)

```bash
# Запустить PostgreSQL
npm run db:up

# Остановить PostgreSQL
npm run db:down

# Перезапустить с очисткой данных
npm run db:reset
```

---

## 🔐 Доступ к админ-панели

После настройки базы данных войдите в админ-панель:

- **URL:** `http://localhost:3000/admin/login`
- **Email:** `admin1@college.am` (или другой из seed данных)
- **Password:** `Admin12345!`

⚠️ **Важно:** После первого входа обязательно измените пароли администраторов!

---

## 🌐 Работа с разных компьютеров

### Да, можно работать с любого компьютера!

Если вы используете базу данных Vercel (Вариант A), то:

✅ **С любого компьютера будет работать одинаково:**

- Все подключаются к одной базе данных на Vercel
- Все видят одни и те же данные
- Не нужно настраивать локальную базу данных
- Просто выполните `vercel link` и `vercel env pull .env.local`

### Инструкция для нового компьютера:

1. Клонируйте репозиторий
2. Установите зависимости: `npm install`
3. Установите Vercel CLI: `npm i -g vercel`
4. Войдите: `vercel login`
5. Подключитесь: `vercel link`
6. Получите переменные: `vercel env pull .env.local`
7. Запустите: `npm run dev`

**Всё!** Работает с того же момента.

---

## 📁 Структура проекта

```
ecaf/
├── app/                    # Next.js App Router
│   ├── (public)/          # Публичные страницы
│   ├── admin/             # Админ-панель
│   ├── api/               # API routes
│   └── components/        # Компоненты
├── lib/                   # Утилиты и конфигурация
│   ├── db.ts             # Prisma клиент
│   ├── auth.ts           # NextAuth конфигурация
│   └── validators/       # Zod схемы валидации
├── prisma/               # Prisma схема и миграции
├── server/               # Server actions
└── public/               # Статические файлы
```

---

## 🔧 Технологии

- **Next.js 16** - React фреймворк
- **TypeScript** - Типизация
- **Prisma** - ORM для работы с БД
- **PostgreSQL** - База данных
- **NextAuth** - Аутентификация
- **Tailwind CSS** - Стилизация
- **Vercel Blob Storage** - Хранение файлов

---

## 📚 Дополнительная документация

- [DEPLOY.md](./DEPLOY.md) - Инструкции по деплою на Vercel
- [SETUP_LOCAL.md](./SETUP_LOCAL.md) - Подробная настройка локальной разработки
- [TEAM_SETUP.md](./TEAM_SETUP.md) - Добавление участников в проект Vercel
- [VERCEL_SETUP.md](./VERCEL_SETUP.md) - Настройка переменных окружения на Vercel
- [CREATE_ADMIN.md](./CREATE_ADMIN.md) - Создание администраторов
- [QUICK_START.md](./QUICK_START.md) - Быстрый старт

---

## ❓ Решение проблем

### Проблема: "User was denied access on the database"

**Решение:** Убедитесь, что `DATABASE_URL` правильно настроен в `.env.local`:

```bash
vercel env pull .env.local
```

### Проблема: "AUTH_SECRET environment variable is required"

**Решение:** Убедитесь, что `NEXTAUTH_SECRET` установлен:

```bash
vercel env pull .env.local
```

### Проблема: Файлы не загружаются

**Решение:** Убедитесь, что на Vercel создан Blob Storage и установлен `BLOB_READ_WRITE_TOKEN`:

```bash
vercel env pull .env.local
```

---

## 👥 Для команды разработчиков

### Добавление нового участника в проект

**Владелец проекта должен:**

1. Зайти на [vercel.com](https://vercel.com) → проект → **Settings** → **Team/Members**
2. Нажать **Add Member** или **Invite**
3. Ввести email участника
4. Выбрать роль **Developer** (рекомендуется)
5. Отправить приглашение

**Новый участник получит email и должен:**

1. Принять приглашение в Vercel
2. Выполнить шаги из раздела "Быстрый старт" выше

📖 **Подробная инструкция:** См. [TEAM_SETUP.md](./TEAM_SETUP.md)

### Первый раз на новом компьютере:

1. `git clone git@github.com:Nely-Hartenyan/ecaf.git && cd ecaf`
2. `npm install`
3. `npm i -g vercel`
4. `vercel login`
5. `vercel link` (выберите проект `ecaf`)
6. `vercel env pull .env.local`
7. `npm run dev`

### Обновление переменных окружения:

Если на Vercel изменились переменные:

```bash
vercel env pull .env.local
```

### Работа с Git:

```bash
# Получить последние изменения
git pull

# Создать новую ветку
git checkout -b feature/my-feature

# Закоммитить изменения
git add .
git commit -m "Описание изменений"
git push origin feature/my-feature
```

---

## ⚠️ Важные замечания

- **НЕ коммитьте** `.env.local` в Git (уже в `.gitignore`)
- При работе с production БД будьте осторожны с данными
- Для экспериментов лучше использовать локальную БД
- Всегда делайте `git pull` перед началом работы

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `npm run dev` покажет ошибки
2. Проверьте переменные окружения: `cat .env.local`
3. Проверьте подключение к БД: `npm run db:studio`
4. Посмотрите документацию в папке проекта
