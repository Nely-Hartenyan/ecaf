# Настройка локальной разработки

## 🚀 Быстрый старт (Рекомендуется: использовать базу данных Vercel)

### Вариант 1: Использовать базу данных Vercel (НЕ нужно запускать локальную БД!)

1. **Установите Vercel CLI** (если еще не установлен):

   ```bash
   npm i -g vercel
   ```

2. **Подключитесь к проекту:**

   ```bash
   vercel link
   ```

3. **Получите переменные окружения из Vercel:**

   ```bash
   vercel env pull .env.local
   ```

4. **Проверьте, что DATABASE_URL установлен:**

   ```bash
   cat .env.local | grep DATABASE_URL
   ```

5. **Запустите проект:**
   ```bash
   npm run dev
   ```

✅ **Готово!** Теперь вы работаете с production базой данных локально, не нужно запускать Docker!

---

## Вариант 2: Использовать локальную базу данных

Если хотите использовать локальную базу данных:

### 1. Запустите Docker Desktop

Убедитесь, что Docker Desktop запущен на вашем Mac.

### 2. Запустите локальную базу данных

```bash
# Запустить PostgreSQL в Docker
npm run db:up
# или
docker-compose up -d
```

### 3. Настройте .env.local

Создайте или отредактируйте `.env.local`:

```env
DATABASE_URL="postgresql://nely@localhost:5432/college_db?schema=public"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="dev-secret-key-change-in-production"
```

### 4. Примените миграции

```bash
# Сгенерировать Prisma Client
npm run db:generate

# Применить миграции
npm run db:migrate
```

### 5. Заполните базу данных (опционально)

```bash
npm run db:seed
```

### 6. Запустите проект

```bash
npm run dev
```

## Файл .env.local

Файл `.env.local` содержит настройки для подключения:

- `DATABASE_URL` - подключение к базе данных (Vercel или локальная)
- `NEXTAUTH_URL` - http://localhost:3000
- `NEXTAUTH_SECRET` - для разработки

## Полезные команды

```bash
# Получить переменные из Vercel (обновить .env.local)
vercel env pull .env.local

# Открыть Prisma Studio (GUI для БД) - работает с любой БД из .env.local
npm run db:studio

# Применить миграции (если используете локальную БД)
npm run db:migrate

# Заполнить базу данных (если используете локальную БД)
npm run db:seed

# Остановить локальную базу данных (если используете Docker)
npm run db:down

# Перезапустить локальную базу данных (если используете Docker)
npm run db:reset
```

## ⚠️ Важно при использовании production БД

- **Будьте осторожны!** Вы работаете с production данными
- Не удаляйте важные данные
- Тестируйте изменения аккуратно
- Для экспериментов лучше использовать локальную БД
