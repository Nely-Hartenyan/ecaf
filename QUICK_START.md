# Быстрый старт для разработки

## 🎯 Использовать базу данных Vercel локально (РЕКОМЕНДУЕТСЯ)

Не нужно запускать Docker! Просто подключитесь к production базе данных.

### Шаг 1: Установите Vercel CLI
```bash
npm i -g vercel
```

### Шаг 2: Подключитесь к проекту
```bash
cd /Users/nely/Desktop/ecaf
vercel link
```

### Шаг 3: Получите переменные окружения
```bash
vercel env pull .env.local
```

Это создаст/обновит файл `.env.local` с переменными из Vercel, включая `DATABASE_URL`.

### Шаг 4: Запустите проект
```bash
npm run dev
```

✅ **Готово!** Теперь вы работаете локально, но с production базой данных.

---

## 🔄 Обновить переменные из Vercel

Если на Vercel изменились переменные окружения:

```bash
vercel env pull .env.local
```

---

## ⚠️ Важно

- Вы работаете с **production данными** - будьте осторожны!
- Не удаляйте важные данные
- Для экспериментов лучше использовать локальную БД

---

## 🐳 Альтернатива: Локальная база данных

Если хотите использовать локальную БД:

1. Запустите Docker Desktop
2. `npm run db:up`
3. Обновите `.env.local` с локальным `DATABASE_URL`
4. `npm run db:migrate`
5. `npm run dev`
