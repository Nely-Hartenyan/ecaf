# ============================================
# Database Configuration
# ============================================

# Для локальной разработки:
DATABASE_URL="postgresql://college_user:college_password@localhost:5432/college_db?schema=public"

# Для Vercel Postgres (в настройках Vercel создается автоматически):
# DATABASE_URL="${POSTGRES_PRISMA_URL}"

# Для внешнего провайдера (Neon, Supabase, Railway и т.д.):
# DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# ============================================
# NextAuth Configuration (ОБЯЗАТЕЛЬНО!)
# ============================================

# URL вашего приложения
# Локально: http://localhost:3000
# На Vercel: https://your-project.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# Секретный ключ для NextAuth (ОБЯЗАТЕЛЬНО для production!)
# Сгенерируйте новый ключ командой: openssl rand -base64 32
# ВАЖНО: Используйте разные ключи для development и production!
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# Альтернатива: можно использовать AUTH_SECRET вместо NEXTAUTH_SECRET
# AUTH_SECRET="your-secret-key-here-change-in-production"

# ============================================
# Vercel Blob Storage (опционально)
# ============================================

# Токен для Vercel Blob Storage
# В локальной разработке файлы сохраняются в public/uploads
# На Vercel создается автоматически при создании Blob Storage
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
